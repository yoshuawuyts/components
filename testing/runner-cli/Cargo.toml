[package]
# Credit: ported from Lann Martin's `wasi:test` prototype
# (https://github.com/lann/wasi-test); see the crate's main.rs header.
# Unpublished and carries no license claim. See testing/README.md "Credits and
# provenance".
name = "wasi-test-runner-cli"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
futures-lite = { version = "2.6.1", default-features = false, features = ["std"] }
wit-bindgen = "0.46.0"
