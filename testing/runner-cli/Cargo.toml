[package]
# Ported from Lann Martin's `wasi:test` prototype (https://github.com/lann/wasi-test).
# Upstream currently has no license; this crate is intentionally unpublished and
# carries no license claim. See testing/README.md "Provenance and licensing".
name = "wasi-test-runner-cli"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
futures-lite = { version = "2.6.1", default-features = false, features = ["std"] }
wit-bindgen = "0.46.0"
