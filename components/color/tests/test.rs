//! Integration tests for the `color` component.
//!
//! The component's behavior is exercised by the native unit tests in
//! `src/lib.rs` (run via `cargo test`) and by the Wasm-component test suite in
//! `testing/` (run via `just test-component color`).
